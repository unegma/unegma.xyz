{
  "name": "blog.rainprotocol.xyz",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "## run this: `bundle install`",
    "add": "## run this: `bundle add`",
    "start": "## run this: `bundle exec jekyll serve`",
    "start-watch": "## run this: `bundle exec jekyll serve --watch`",
    "deploy": "aws --profile unegma s3 sync --delete ./_site/ s3://blog.rainprotocol.xyz/"
  }
}
