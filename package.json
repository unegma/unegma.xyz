{
  "name": "blog.rainprotocol.xyz",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "bundle install",
    "start": "bundle exec jekyll serve",
    "deploy": "npm run build; aws --profile unegma s3 sync --delete ./_site/ s3://blog.rainprotocol.xyz/"
  }
}
